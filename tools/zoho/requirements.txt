# Zoho API helpers — install: pip install -r requirements.txt
# zohocrmsdk8-0 5.x pins requests==2.32.4; keep in sync when upgrading the SDK.
requests==2.32.4
python-dotenv>=1.0.0
# Official Zoho CRM REST wrapper for API v8 (OAuth + typed calls). See:
# https://www.zoho.com/crm/developer/docs/sdk/server-side/python-sdk.html
zohocrmsdk8-0>=5.0.0,<6
pymupdf>=1.24.0
Pillow>=10.0.0
