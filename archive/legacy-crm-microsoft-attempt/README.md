# Legacy Microsoft CRM attempt (Dataverse, canvas, custom frontend notes)

- **`solutions/scrCRMDataverse/`** — Dataverse solution project (unbuilt sources only; `bin`/`obj` may be absent).
- **`tools/dataverse/*.py`** — Web API scripts for the `new_Opportunity` table and seeds. Recreate `venv/` locally; see the script headers and original `tools/dataverse/README.md` in Git history or restore from the first commit that contained it.
- **Root `*.md` files** here — architecture and pivot documentation from the pre-Zoho period.

**Not** part of the active Zoho work unless you explicitly reopen the Microsoft path.
