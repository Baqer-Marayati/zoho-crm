# Archive (legacy)

This folder is **read-only history**. Nothing here is required for the current **Zoho CRM + Power BI** direction.

| Folder | Contents |
|--------|----------|
| `legacy-approvals-microsoft-starter/` | Original **SharePoint + Power Automate + Canvas** starter: guides and `ORIGINAL-README.md` from the pre-CRM phase |
| `legacy-crm-microsoft-attempt/` | **Dataverse** solution, **Python** Dataverse tools (no `venv` stored), and **docs** for canvas / custom React on Dataverse pivots |

## If you un-archive

Copy files back to a new branch, update paths, and re-test before discarding the Zoho-first layout. Prefer **new** Zoho documentation under `docs/zoho/` instead of reviving the Power Apps pipeline unless you have a hard requirement.

## Size note

`venv/` and `bin`/`obj` build output are **not** kept in git in the archived tool paths. Recreate a virtual environment if you need to run the old Python scripts.
